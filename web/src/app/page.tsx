export default function Home() {
  return (
    <main className="mx-auto max-w-5xl px-4 py-10">
      <h1 className="text-2xl font-semibold tracking-tight">High-Tail Atlas</h1>
      <p className="mt-4 max-w-3xl text-stone-800">
        Modeled estimates of the population share at IQ ≥ 130. Dashboard views
        (map, comparison chart, and table) are not in this scaffold yet. Country
        means are estimates; the figures will be model output, not a census.
      </p>
    </main>
  );
}
